const Discord = require("discord.js");
const fs = require("fs");
module.exports = async () => {
  GuardLLLClient.user.setPresence({ activity: { name: config.GuardIIIActivity }, status: "idle" });
  }; 
  module.exports.g3config = {
      name: "ready"
    }