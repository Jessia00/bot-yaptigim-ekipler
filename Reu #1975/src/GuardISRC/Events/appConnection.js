const Discord = require("discord.js");
const fs = require("fs");
module.exports = async () => {
  GuardLClient.user.setPresence({ activity: { name: config.GuardIActivity }, status: "idle" });
  }; 
  module.exports.g1config = {
      name: "ready"
    }