const Discord = require("discord.js");
const fs = require("fs");
module.exports = async () => {
  ChatGuardClient.user.setPresence({ activity: { name: config.ChatGuardActivity }, status: "idle" });
  }; 
  module.exports.configim = {
      name: "ready"
    }