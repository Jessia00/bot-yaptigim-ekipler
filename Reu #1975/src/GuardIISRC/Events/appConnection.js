const Discord = require("discord.js");
const fs = require("fs");
module.exports = async () => {
  GuardLLClient.user.setPresence({ activity: { name: config.GuardIIActivity }, status: "idle" });
  }; 
  module.exports.g2config = {
      name: "ready"
    }